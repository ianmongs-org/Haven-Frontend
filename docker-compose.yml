services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        - VITE_API_BASE_URL=${VITE_API_BASE_URL:-http://localhost:8081/api}
        - VITE_WS_BASE_URL=${VITE_WS_BASE_URL:-wss://haven-drauhuadfhf9bjby.southafricanorth-01.azurewebsites.net/ws/chat}
        - VITE_PROD_API_BASE_URL=${VITE_PROD_API_BASE_URL:-https://haven-drauhuadfhf9bjby.southafricanorth-01.azurewebsites.net}
    ports:
      - "3005:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
